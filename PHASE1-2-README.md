# Phase 1 & 2 — handover

The Phase 1 and Phase 2 work was completed and verified, but the session's git
proxy stopped authorising writes partway through (`401` on `git-receive-pack`),
so the commits could not be pushed normally. `phase1-2-series.patch` in this
directory carries both commits, with their full messages.

## Apply

```bash
git checkout claude/solarsystem-repo-analysis-n58f8n
git am phase1-2-series.patch
```

That reproduces two commits:

1. **Phase 0+1** — WebGL2 context and volumetric lit-dust renderer
2. **Phase 2** — GPU-resident particle-mesh layer with emergent spiral structure

Net change to `index.html`: **+987 / −14**.

## What the work does

**Phase 0 — WebGL2 groundwork.** The context ladder prefers WebGL2, with WebGL1
kept as a complete fallback behind an `isGL2` flag. Existing GLSL 1.00 programs
compile unchanged; only new programs use GLSL ES 3.00. HDR targets use sized
internal formats on GL2.

**Phase 1 — lit volumetric dust.** Previously dust could only *subtract* light:
it was a dark multiplicative billboard, so it silhouetted but never glowed. Real
dust also scatters starlight. A 3D volume (RGB = stellar emissivity, A = dust
extinction) is mip-blurred to approximate irradiance and raymarched with single
scattering, self-shadowing and a forward phase function toward the core.

**Phase 2 — GPU particle-mesh layer.** A second population living entirely in
float textures: deposit → Jacobi Poisson → gradient → symplectic integration →
direct point rendering, up to 4.2M particles. It moves under the analytic
axisymmetric field plus particle-mesh self-gravity from its own density field, so
its spiral structure is not drawn — it grows from gravity. This is a grid/Poisson
method; there is deliberately no Barnes-Hut.

## Verification

- **Emergent structure measured, not asserted:** with self-gravity the m=2 Fourier
  amplitude of the surface density rises to 0.083 and sustains 0.065–0.083,
  against a 0.030 shot-noise floor that the self-gravity-off control decays back
  to. The disc also expands less (max radius 791 vs 817), consistent with the
  extra binding mass.
- **Stability:** no NaN and no particle ejection over 630 steps. Self-gravity is
  capped at 35% of the mean field, mirroring `LG_CAP` in the CPU solver.
- **Regression:** all 7 observation modes, both new subsystems toggled on and off,
  GPU stars on/off, and a full restart — 0 console errors.

## Risk

Both systems are feature-gated and degrade cleanly. The volumetric renderer falls
back to the existing sprite dust on WebGL1 or if the volume fails to initialise.
The GPU layer is **off by default** and purely additive — the existing simulation
is untouched, and any initialisation failure disables the layer.

## New controls

- *Cinematic Rendering*: Lit Volumetric Dust, plus Scattering, Volume Density and
  Raymarch Quality.
- *Core Physics Variables*: Emergent Dynamics Layer, particle count
  (65k / 262k / 1.0M / 4.2M) and self-gravity strength.

---
_Generated by [Claude Code](https://claude.ai/code)_
